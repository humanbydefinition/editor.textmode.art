{
  "name": "synth.textmode.art",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "author": "humanbydefinition <hello@textmode.art> (https://code.textmode.art)",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w @synth.textmode.art/contracts && npm run build -w client && npm run build -w server",
    "check-types": "npm run check-types -w @synth.textmode.art/contracts && npm run build -w @synth.textmode.art/contracts && npm run check-types -w client && npm run check-types -w server",
    "server:dev": "npm run dev -w server",
    "server:build": "npm run build -w @synth.textmode.art/contracts && npm run build -w server",
    "lint": "npm run lint -w client && npm run lint -w server",
    "format": "prettier --write \"**/*.{ts,js}\"",
    "format:check": "prettier --check \"**/*.{ts,js}\""
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
