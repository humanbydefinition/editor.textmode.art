import { StrudelEngine } from '../engines/strudel/StrudelEngine';
import { createRunner } from '../shared/createRunner';

createRunner(StrudelEngine, 'Strudel runner is running in top-level window (debug mode).');
